<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    <!-- Strip self-update permission for Google Play distribution.
         Play Console flags REQUEST_INSTALL_PACKAGES as a sensitive
         permission requiring a declaration; the playStore flavor must
         never request it (updates come via Play itself). -->
    <uses-permission
        android:name="android.permission.REQUEST_INSTALL_PACKAGES"
        tools:node="remove" />

    <!-- If Phone Link permissions in src/main/AndroidManifest.xml are ever uncommented
         (e.g., for sideload builds), uncomment the removals below so playStore builds never include them:
    <uses-permission
        android:name="android.permission.READ_SMS"
        tools:node="remove" />
    <uses-permission
        android:name="android.permission.SEND_SMS"
        tools:node="remove" />
    <uses-permission
        android:name="android.permission.RECEIVE_SMS"
        tools:node="remove" />
    <uses-permission
        android:name="android.permission.READ_CALL_LOG"
        tools:node="remove" />
    <uses-permission
        android:name="android.permission.READ_CONTACTS"
        tools:node="remove" />
    -->
</manifest>
